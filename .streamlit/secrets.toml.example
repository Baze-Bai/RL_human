# Streamlit Cloud secrets — paste into the app's Secrets settings
# (Settings > Secrets in the Streamlit Cloud dashboard)

DASHSCOPE_API_KEY = "sk-..."

# Optional: Supabase
SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-anon-key"
